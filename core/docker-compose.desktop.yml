services:
  n8n:
    ports:
      - "5678:5678"
  ollama:
    ports:
      - "11434:11434"
  qdrant:
    ports:
      - "6333:6333"
  stt:
    ports:
      - "8000:8000"
  tts:
    ports:
      - "7860:7860"

# Development override for Docker Desktop on Windows.
# Public ports are intentionally enabled only here for local diagnostics.
