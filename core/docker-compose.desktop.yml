services: {}

# Docker Desktop production-like override.
# Internal services are intentionally NOT published on the Windows host.
# Access the n8n editor through Caddy at https://assis.localhost.
# For temporary loopback-only diagnostics, add core/docker-compose.diagnostics.yml.
